/**
 * Defines the checked Exceptions that may occur during execution. All of them
 * are a concrete subclass of the {@link runtime.exceptions.ExecutionException
 * ExecutionException}. Exceptions are used (instead of simple error messages)
 * to allow for uniform handling of errors during parsing and clearer control
 * flow.
 *
 * @author Alex Mandelias
 */
package runtime.exceptions;
